services:
  # gr-dir:
  #   image: busybox
  #   entrypoint: cat /file
  #   volumes:
  #     - ./configs/${PORTAINER_EDGE_GROUP}/file:/file

  gr-file:
    image: busybox
    entrypoint: watch cat /file
    volumes:
      - ./configs/${PORTAINER_EDGE_GROUP}:file.txt

  # id-dir:
  #   image: busybox
  #   entrypoint: cat /file
  #   volumes:
  #     - ./configs/${PORTAINER_EDGE_ID}/file:/file

  id-file:
    image: busybox
    entrypoint: watch cat /file
    volumes:
      - ./configs/${PORTAINER_EDGE_ID}:file.txt